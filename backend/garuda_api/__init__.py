"""GarudaChip web backend — FastAPI app exposing the prompt→RTL→GDSII pipeline as a
professional Vite frontend would consume it: persistent chats, streaming runs, image/
PDF uploads, and a knowledge store that updates live as a run progresses."""
